/**
 * @file config_user_template.h
 * @brief Шаблон конфігурації проекту для ServoLib
 * @author Your Name
 * @date 2025
 *
 * ІНСТРУКЦІЯ З ВИКОРИСТАННЯ:
 * 1. Скопіюйте цей файл у директорію Inc/ вашого проекту
 * 2. Перейменуйте на "config_user.h"
 * 3. Розкоментуйте та налаштуйте потрібні параметри
 * 4. Параметри, які ви НЕ визначите, будуть взяті з config_defaults.h
 *
 * СТРУКТУРА ПРОЕКТУ:
 * YourProject/
 * ├── Inc/
 * │   ├── config_user.h     <-- Цей файл (перейменований)
 * │   └── main.h
 * ├── Src/
 * │   └── main.c
 * └── Libs/ServoLib/        <-- Бібліотека (без змін)
 *
 * НАЛАШТУВАННЯ STM32CubeIDE:
 * Project Properties → C/C++ Build → Settings → Include paths:
 * - ../ServoLib/Inc
 * - ../ServoLib/Board/STM32F411  (або PC_Emulation для емуляції)
 * - Inc  (де знаходиться config_user.h)
 */

#ifndef CONFIG_USER_H
#define CONFIG_USER_H

#ifdef __cplusplus
extern "C" {
#endif

/* =============================================================================
 * ПРИКЛАДИ КОНФІГУРАЦІЙ ДЛЯ РІЗНИХ ТИПІВ ПРОЕКТІВ
 * ===========================================================================*/

/*
 * Розкоментуйте ОДИН з наведених нижче шаблонів, або створіть свій власний.
 */

/* -----------------------------------------------------------------------------
 * ПРИКЛАД 1: МАЛОПОТУЖНИЙ РОБОТ (стандартні дефолти)
 * ---------------------------------------------------------------------------*/
/*
// Мотор
#define MOTOR_MAX_CURRENT 2000U        // 2A для малого DC мотора
#define MOTOR_OVERCURRENT_LIMIT 2500U  // 2.5A захист
#define MOTOR_MAX_TEMPERATURE 85       // 85°C

// PID налаштування
#define DEFAULT_PID_KP 2.5f
#define DEFAULT_PID_KI 0.3f
#define DEFAULT_PID_KD 0.1f

// Сенсор
#define ENCODER_RESOLUTION_BITS 12     // AS5600 12-bit I2C енкодер

// Керування
#define DEFAULT_MAX_VELOCITY 180.0f    // 0.5 обертів/сек
#define DEFAULT_MAX_ACCELERATION 360.0f
*/

/* -----------------------------------------------------------------------------
 * ПРИКЛАД 2: ПРОМИСЛОВИЙ СЕРВОПРИВІД (override абсолютних меж)
 * ---------------------------------------------------------------------------*/
/*
// OVERRIDE абсолютних меж для потужного обладнання!
#define ABSOLUTE_MAX_CURRENT 20000U    // 20A - промисловий мотор
#define ABSOLUTE_MAX_TEMP 150          // 150°C - промислова електроніка

// Мотор
#define MOTOR_MAX_CURRENT 15000U       // 15A номінал
#define MOTOR_OVERCURRENT_LIMIT 18000U // 18A захист
#define MOTOR_MAX_TEMPERATURE 120      // 120°C

// Високоточний сенсор
#define ENCODER_RESOLUTION_BITS 18     // AEAT-9922 18-bit SPI енкодер

// Швидкісне керування
#define DEFAULT_MAX_VELOCITY 720.0f    // 2 оберти/сек
#define DEFAULT_MAX_ACCELERATION 1440.0f

// Більший буфер логування
#define ERROR_LOG_BUFFER_SIZE 128

// Увімкнути статистику продуктивності
#define ENABLE_PERFORMANCE_STATS 1
*/

/* -----------------------------------------------------------------------------
 * ПРИКЛАД 3: ПРЕЦИЗІЙНИЙ ПОЗИЦІОНЕР (точне керування)
 * ---------------------------------------------------------------------------*/
/*
// Мотор з редуктором
#define MOTOR_MAX_CURRENT 1500U        // 1.5A для мотора з редуктором
#define MOTOR_OVERCURRENT_LIMIT 2000U

// Високоточний PID
#define DEFAULT_PID_KP 5.0f            // Більший коефіцієнт для точності
#define DEFAULT_PID_KI 0.5f
#define DEFAULT_PID_KD 0.2f

// Малий deadband для точності
#define POSITION_DEADBAND 0.05f        // 0.05° deadband
#define POSITION_ERROR_THRESHOLD 0.1f  // 0.1° точність

// Повільне, плавне керування
#define DEFAULT_MAX_VELOCITY 90.0f     // 0.25 обертів/сек
#define DEFAULT_MAX_ACCELERATION 180.0f
#define MAX_JERK 500.0f                // Малий ривок для плавності

// Обов'язково S-криві траєкторії
#define DEFAULT_TRAJECTORY_TYPE 1      // S-curve
*/

/* =============================================================================
 * ВАША ВЛАСНА КОНФІГУРАЦІЯ
 * ===========================================================================*/

/* Розкоментуйте та налаштуйте параметри нижче для вашого проекту */

/* -----------------------------------------------------------------------------
 * Абсолютні межі обладнання (override тільки якщо потрібно!)
 * ---------------------------------------------------------------------------*/

/* УВАГА: Змінюйте тільки якщо ваше обладнання відрізняється від типового! */

// #define ABSOLUTE_MAX_CURRENT 10000U    // Абсолютний максимум струму (mA)
// #define ABSOLUTE_MAX_TEMP 125          // Абсолютний максимум температури (°C)
// #define ABSOLUTE_MAX_VELOCITY 3600.0f  // Абсолютний максимум швидкості (град/с)

/* -----------------------------------------------------------------------------
 * Параметри безпеки двигуна
 * ---------------------------------------------------------------------------*/

// #define MOTOR_MAX_CURRENT 2000U        // Максимальний струм двигуна (mA)
// #define MOTOR_OVERCURRENT_LIMIT 2500U  // Поріг захисту (має бути > MOTOR_MAX_CURRENT)
// #define MOTOR_MAX_TEMPERATURE 85       // Максимальна температура (°C)
// #define MOTOR_STARTUP_DELAY_MS 10      // Затримка старту (ms)
// #define MOTOR_WATCHDOG_TIMEOUT 100     // Таймаут watchdog (ms)

/* -----------------------------------------------------------------------------
 * Конфігурація сенсора
 * ---------------------------------------------------------------------------*/

// #define ENCODER_RESOLUTION_BITS 12     // Роздільність енкодера (12 для AS5600, 18 для AEAT-9922)
// #define SENSOR_I2C_TIMEOUT 10          // Таймаут I2C (ms)
// #define SENSOR_SPI_TIMEOUT 10          // Таймаут SPI (ms)
// #define SENSOR_READ_RETRIES 3          // Кількість повторів при помилці
// #define SENSOR_INIT_DELAY_MS 50        // Затримка ініціалізації (ms)

/* -----------------------------------------------------------------------------
 * PID регулятор
 * ---------------------------------------------------------------------------*/

// #define DEFAULT_PID_KP 1.0f            // Пропорційний коефіцієнт
// #define DEFAULT_PID_KI 0.1f            // Інтегральний коефіцієнт
// #define DEFAULT_PID_KD 0.05f           // Диференціальний коефіцієнт
// #define PID_INTEGRAL_MAX 100.0f        // Максимум інтегральної складової (anti-windup)
// #define PID_INTEGRAL_MIN -100.0f       // Мінімум інтегральної складової
// #define PID_OUTPUT_MAX 100.0f          // Максимальний вихід PID
// #define PID_OUTPUT_MIN -100.0f         // Мінімальний вихід PID

/* -----------------------------------------------------------------------------
 * Межі керування положенням
 * ---------------------------------------------------------------------------*/

// #define DEFAULT_MIN_POSITION 0.0f      // Мінімальна позиція (градуси)
// #define DEFAULT_MAX_POSITION 360.0f    // Максимальна позиція (градуси)
// #define DEFAULT_MAX_VELOCITY 180.0f    // Максимальна швидкість (град/с)
// #define DEFAULT_MAX_ACCELERATION 360.0f // Максимальне прискорення (град/с²)
// #define POSITION_DEADBAND 0.1f         // Зона нечутливості (градуси)
// #define POSITION_ERROR_THRESHOLD 0.5f  // Поріг досягнення цілі (градуси)

/* -----------------------------------------------------------------------------
 * Генератор траєкторій
 * ---------------------------------------------------------------------------*/

// #define ENABLE_TRAJECTORY_GEN 1        // Увімкнути генератор траєкторій
// #define DEFAULT_TRAJECTORY_TYPE 1      // 0=лінійна, 1=S-крива
// #define MAX_JERK 1000.0f               // Максимальний ривок (град/с³)

/* -----------------------------------------------------------------------------
 * Системна конфігурація
 * ---------------------------------------------------------------------------*/

// #define CONTROL_LOOP_FREQ 1000U        // Частота контуру керування (Hz)
// #define PWM_FREQUENCY 1000U            // Частота PWM (Hz)
// #define PWM_RESOLUTION 1000U           // Роздільна здатність PWM (кроки)

/* -----------------------------------------------------------------------------
 * Функції безпеки
 * ---------------------------------------------------------------------------*/

// #define ENABLE_POSITION_LIMITS 1       // Перевірка меж положення
// #define ENABLE_VELOCITY_LIMITS 1       // Обмеження швидкості
// #define ENABLE_CURRENT_PROTECTION 1    // Струмовий захист
// #define ENABLE_THERMAL_PROTECTION 1    // Температурний захист
// #define ENABLE_WATCHDOG 1              // Watchdog таймер
// #define EMERGENCY_STOP_TIME_MS 10      // Час аварійної зупинки (ms)

/* -----------------------------------------------------------------------------
 * Конфігурація гальм
 * ---------------------------------------------------------------------------*/

// #define BRAKE_RELEASE_DELAY_MS 100     // Затримка відпускання гальм (ms)
// #define BRAKE_ENGAGE_TIMEOUT_MS 3000   // Таймаут блокування гальм (ms)
// #define BRAKE_IDLE_TIMEOUT_MS 5000     // Таймаут бездіяльності (ms)

/* -----------------------------------------------------------------------------
 * Налагодження та логування
 * ---------------------------------------------------------------------------*/

// #define ENABLE_DEBUG 1                 // Налагоджувальні повідомлення
// #define ENABLE_ERROR_LOGGING 1         // Логування помилок
// #define ERROR_LOG_BUFFER_SIZE 32       // Розмір буфера логування
// #define ENABLE_PERFORMANCE_STATS 0     // Статистика продуктивності

/* -----------------------------------------------------------------------------
 * Конфігурація пам'яті
 * ---------------------------------------------------------------------------*/

// #define MAX_AXES 6                     // Максимальна кількість осей
// #define AXIS_STACK_SIZE 1024           // Розмір стеку для осі (байт)
// #define USE_STATIC_MEMORY 1            // Статична пам'ять (без malloc)

/* -----------------------------------------------------------------------------
 * Підтримка функціональності
 * ---------------------------------------------------------------------------*/

// #define SUPPORT_VELOCITY_MODE 1        // Режим швидкісного керування
// #define SUPPORT_TORQUE_MODE 0          // Режим моментного керування
// #define SUPPORT_AUTO_CALIBRATION 1     // Автоматичне калібрування
// #define SUPPORT_SETTINGS_SAVE 0        // Збереження налаштувань

/* =============================================================================
 * ПРИМІТКИ
 * ===========================================================================*/

/*
 * 1. Параметри з #ifndef в config_defaults.h можуть бути перевизначені
 * 2. Визначайте ТІЛЬКИ параметри, які відрізняються від дефолтів
 * 3. Див. config_defaults.h для повного списку доступних параметрів
 * 4. Див. config_lib.h для математичних констант (НЕ можуть бути override)
 * 5. Компілятор виведе інформаційні повідомлення про використану конфігурацію
 */

#ifdef __cplusplus
}
#endif

#endif /* CONFIG_USER_H */
