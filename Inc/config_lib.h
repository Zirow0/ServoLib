/**
 * @file config_lib.h
 * @brief Універсальні константи бібліотеки ServoCore
 * @author ServoCore Team
 * @date 2025
 *
 * Цей файл містить тільки математичні константи та конверсії, які є
 * універсальними для всіх проектів і платформ. Ці константи НЕ можуть
 * бути перевизначені користувачем.
 *
 * НЕ ВКЛЮЧАЄ:
 * - Валідаційні межі (різні для різного обладнання)
 * - Розміри буферів (можуть бути різні для різних проектів)
 * - Будь-які параметри, специфічні для обладнання
 */

#ifndef SERVOCORE_CONFIG_LIB_H
#define SERVOCORE_CONFIG_LIB_H

#ifdef __cplusplus
extern "C" {
#endif

/* =============================================================================
 * Математичні константи
 * ===========================================================================*/

/** @brief Математична константа PI */
#define MATH_PI 3.14159265358979323846f

/** @brief Подвоєне PI (2π) */
#define MATH_2PI (2.0f * MATH_PI)

/** @brief Половина PI (π/2) */
#define MATH_PI_2 (MATH_PI / 2.0f)

/* =============================================================================
 * Конверсії кутових одиниць
 * ===========================================================================*/

/** @brief Конверсія градусів в радіани */
#define DEG_TO_RAD (MATH_PI / 180.0f)

/** @brief Конверсія радіан в градуси */
#define RAD_TO_DEG (180.0f / MATH_PI)

/* =============================================================================
 * Конверсії швидкості обертання
 * ===========================================================================*/

/** @brief Конверсія оберти/хв (RPM) в радіани/сек */
#define RPM_TO_RAD_PER_SEC (MATH_2PI / 60.0f)

/** @brief Конверсія радіани/сек в оберти/хв (RPM) */
#define RAD_PER_SEC_TO_RPM (60.0f / MATH_2PI)

/** @brief Конверсія градуси/сек в оберти/хв (RPM) */
#define DEG_PER_SEC_TO_RPM (60.0f / 360.0f)

/** @brief Конверсія оберти/хв (RPM) в градуси/сек */
#define RPM_TO_DEG_PER_SEC (360.0f / 60.0f)

/* =============================================================================
 * Конверсії часу
 * ===========================================================================*/

/** @brief Мілісекунди в секунди */
#define MS_TO_SEC 0.001f

/** @brief Секунди в мілісекунди */
#define SEC_TO_MS 1000.0f

/** @brief Мікросекунди в секунди */
#define US_TO_SEC 0.000001f

/** @brief Секунди в мікросекунди */
#define SEC_TO_US 1000000.0f

/* =============================================================================
 * Конверсії електричних одиниць
 * ===========================================================================*/

/** @brief Міліампери в ампери */
#define MA_TO_A 0.001f

/** @brief Ампери в міліампери */
#define A_TO_MA 1000.0f

/* =============================================================================
 * Стандарт C99 та сумісність компіляторів
 * ===========================================================================*/

/* Перевірка стандарту C */
#if !defined(__STDC_VERSION__) || __STDC_VERSION__ < 199901L
    #warning "This library requires C99 or later"
#endif

/* Підтримка bool для компіляторів без stdbool.h */
#ifndef __cplusplus
    #if !defined(__GNUC__) && !defined(_MSC_VER)
        #ifndef bool
            #define bool _Bool
            #define true 1
            #define false 0
        #endif
    #endif
#endif

#ifdef __cplusplus
}
#endif

#endif /* SERVOCORE_CONFIG_LIB_H */
